
" Search for the filename we came from, works only once.
" TODO: 
"   Make it work recursively
"   Don't break <c-o>
"   Add ability to get visible directory
" nnoremap - <Plug>VinegarUp<CR>/<C-R>=expand("%")<CR><CR>

" allow to cd and track current directory with b:netrw_curdir
" let g:netrw_keepdir=0
